<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="hidden_invoice_messaging.list">
      <field name="name">hidden_invoice_messaging list</field>
      <field name="model">hidden_invoice_messaging.hidden_invoice_messaging</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="hidden_invoice_messaging.action_window">
      <field name="name">hidden_invoice_messaging window</field>
      <field name="res_model">hidden_invoice_messaging.hidden_invoice_messaging</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="hidden_invoice_messaging.action_server">
      <field name="name">hidden_invoice_messaging server</field>
      <field name="model_id" ref="model_hidden_invoice_messaging_hidden_invoice_messaging"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="hidden_invoice_messaging" id="hidden_invoice_messaging.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="hidden_invoice_messaging.menu_1" parent="hidden_invoice_messaging.menu_root"/>
    <menuitem name="Menu 2" id="hidden_invoice_messaging.menu_2" parent="hidden_invoice_messaging.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="hidden_invoice_messaging.menu_1_list" parent="hidden_invoice_messaging.menu_1"
              action="hidden_invoice_messaging.action_window"/>
    <menuitem name="Server to list" id="hidden_invoice_messaging" parent="hidden_invoice_messaging.menu_2"
              action="hidden_invoice_messaging.action_server"/>
    -->
  </data>
</odoo>